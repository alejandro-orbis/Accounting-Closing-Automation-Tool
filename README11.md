# Accounting-Closing-Automation-Tool
Python tool to automate the monthly accounting closing process. Reads CSV files exported from accounting systems, applies configurable rules (suppliers, keywords), classifies transactions, identifies exceptions, and generates auditable reports in Excel and CSV. Never modifies the original data.
